import pandas as pd

def price_to_book(price: pd.Series, book_value_per_share: pd.Series) -> pd.Series:
    """
    Compute Price/Book ratio. Assumes book value is lagged appropriately.
    """
    return price / book_value_per_share
